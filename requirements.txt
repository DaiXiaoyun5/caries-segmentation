# Install a PyTorch build matching the CUDA driver separately:
# https://pytorch.org/get-started/locally/
torch
torchvision

# Core training and evaluation
numpy
Pillow
matplotlib
opencv-python
albumentations
segmentation-models-pytorch

# Reproducible SegFormer-B2 comparison baseline
transformers==4.50.0

# Official Swin-Unet adapter (third-party source/weights stay outside Git)
yacs
PyYAML
timm
einops

# Optional model-complexity reporting used by some scripts
thop

# Optional experiment families are intentionally not included:
# nnunetv2, ultralytics, MedSAM/segment-anything, and RWKV dependencies.
