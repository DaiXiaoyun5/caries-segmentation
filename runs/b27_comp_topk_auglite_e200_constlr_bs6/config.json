{
  "run_name": "b27_comp_topk_auglite_e200_constlr_bs6",
  "image_size": 512,
  "augment_profile": "lite",
  "batch_size": 6,
  "epochs": 200,
  "lr": 0.0003,
  "weight_decay": 0.0001,
  "num_workers": 2,
  "seed": 42,
  "encoder_name": "resnet34",
  "encoder_weights": "imagenet",
  "component_topk_ratio": 0.3,
  "component_gamma": 2.0,
  "component_weight": 0.03,
  "component_start_epoch": 40,
  "model_class": "ResNet34UNetBRRB27CompTopK",
  "experiment": "B27: B2 + component-level top-k recall loss",
  "note": "B27 keeps the exact B2 architecture and B2 boundary/rim auxiliary loss. It adds a lightweight component-level top-k recall loss after warm-up to encourage each GT component, especially small lesions, to contain at least confident predicted pixels. The loss is intentionally weak and does not use Tversky, SDF, consistency, or additional modules.",
  "b2_unchanged": {
    "boundary_target": "dilate(mask,3)-erode(mask,3)",
    "rim_target": "dilate(mask,9)-dilate(mask,3)",
    "rbsg_lambda_pos": 0.2,
    "rbsg_lambda_rim": 0.1,
    "epoch_1_to_20": "L_seg + 0.10*L_boundary + 0.05*L_rim",
    "epoch_21_to_end": "L_seg + 0.20*L_boundary + 0.10*L_rim"
  },
  "component_topk_loss": {
    "definition": "For each GT connected component, maximize mean top-k predicted probabilities inside the component.",
    "topk_ratio": 0.3,
    "gamma": 2.0,
    "weight": 0.03,
    "start_epoch": 40,
    "area_weight": "1/sqrt(area), normalized per image",
    "component_labeling": "scipy.ndimage.label -> cv2.connectedComponents -> numpy BFS fallback",
    "objective": "reduce missed small lesion components without forcing a global precision/recall shift"
  },
  "augmentation": {
    "type": "online",
    "profile": "lite",
    "train_only": true
  }
}